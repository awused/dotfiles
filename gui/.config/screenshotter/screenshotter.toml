# Used for desktop screenshots and when the window name can't be determined
Fallback = 'desktop'
fallback = 'desktop'
ScreenshotDir = '/storage/public/shots'
screenshot_dir = '/storage/public/shots'

# Will be ignored in favour of any available children
# Note that gnome-terminal is uniquely incapable and doesn't set WINDOWID
IgnoredParents = ['kitty', 'xfce4-terminal', 'zsh', 'alacritty', 'bash', 'kthreadadd']
ignored_parents = ['kitty', 'xfce4-terminal', 'zsh', 'alacritty', 'bash', 'kthreadadd']

# Uncomment to check for WINDOWID if you're using a terminal that doesn't use
# separate processes per window. Doesn't help with gnome-terminal.
CheckWindowID = true

# Callback will be called with the path to screenshot.
Callback = 'public-clipboard'
callback = 'public-clipboard'

# File format
# Either png or webp, webp is always lossless.
# png is recommended but webp compresses much faster and slightly better.
format = "png"

# PNG compression level in the range of 0-9
# 0 is fastest, 9 is slowest
# 8 and 9 are slow enough to be annoying for interactive usage
# 0 compresses too poorly to use and saves little time over 1
Compression = 5
compression = 5

# Additional shaders to use with slop.
# See https://github.com/naelstrof/slop/tree/master/shaderexamples
SlopShaders = ['boxzoom']

# Temporarily enable Mouse Keys during slop region selection.
# This will enable and then disable mouse keys unless it is already enabled.
# Requires a version of slop with Mouse Keys support
MouseKeys = true

# Timeout in seconds.
# Set to 0 to disable.
timeout = 120

# A list of monitor positions in _physical_ pixels.
# Used to correct positions with unscaled output when it covers multiple monitors with different
# scales. Not necessary if all monitors share the same scales.
#
# id is some unique fragment of the monitor's description.
# Recommended to pick the serial number, if available.
# See `hyprctl monitors | grep description`, `swaymsg -t get_outputs | grep serial`,
#     or `RUST_LOG=Trace screenshotter name | grep Description`
# If the id matches multiple monitors, this will give bad results.
#
# x and y are the integer position of the visible upper left corner of the monitor.
# The same values you'd have passed to xrander --pos if no scaling is applied.
# Positions can be negative.
monitor_positions = [
  {id = "20480B000000", x = 2560, y = 1440 },
  {id = "19050B000191", x = 0, y = 2160 },
  {id = "19050B000199", x = 6400, y = 2160 },
  {id = "19050B000068", x = 3200, y = 0 },
]


# Overrides. The first one with a matching name and Regex is used.
#
# Name: The application name to match against. Multiple overrides can be
# specified for a name.
#
# Regex: If present, the full command line is matched against this RE.
# Leave blank to match every instance of Name. Regex can't match every window,
# some windows have no pids so we use the name instead.
#
# Format: The output pattern. Use {n} to reference the groups from any regex
# match.

[[OverridesX]]
  Name = "vimx"
  Format = "vim"

[[OverridesX]]
  Name = "gvim"
  Format = "vim"

[[OverridesX]]
  Name = "nvim"
  Format = "vim"

[[OverridesX]]
  Name = "i3"
  Format = "desktop"

[[OverridesX]]
  Name = "chromium-browser"
  Format = "chromium"

[[OverridesX]]
  Name = "chromium-freeworld"
  Format = "chromium"

[[OverridesX]]
  Regex = '(E|e)muera'
  Format = "emuera"

[[OverridesX]]
  Name = "wine32-preloader"
  Regex = 'TLS.*Game.exe$'
  Format = "the-last-sovereign"

[[OverridesX]]
  Name = "wine32-preloader"
  Regex = '^(.*(/|\\))?([^/]+) \d\.\d\d\d[[:lower:]]?\\Game.exe'
  Format = "%[4]s"

[[OverridesX]]
  Name = "wine32-preloader"
  Regex = '^(.*(/|\\))?([^/]+) ?\d\.?\d\.?\d[[:lower:]]?\\Game.exe'
  Format = "%[4]s"

[[OverridesX]]
  Name = "wine32-preloader"
  Regex = '^(.*(/|\\))?([^/]+)\\Game.exe'
  Format = "%[4]s"

[[OverridesX]]
  Name = "wine32-preloader"
  Regex = '^(.*(/|\\))?([^/\ ]+)\.exe'
  Format = "%[4]s"

[[OverridesX]]
  Name = "wine64-preloader"
  Regex = '^(.*(/|\\))?([^/]+) \d\.\d\d\d[[:lower:]]?\\Game.exe'
  Format = "%[4]s"

[[OverridesX]]
  Name = "wine64-preloader"
  Regex = '^(.*(/|\\))?([^/]+) ?\d\.?\d\.?\d[[:lower:]]?\\Game.exe'
  Format = "%[4]s"

[[OverridesX]]
  Name = "wine-preloader"
  Regex = '^(.*(/|\\))?([^/]+) v?(\d+\.)*\d+[[:lower:]]?\\Game.exe'
  Format = "%[4]s"

[[OverridesX]]
  Name = "wine-preloader"
  Regex = '^(.*(/|\\))?([^/]+) (\d+\.)*\d+[[:lower:]]?\\Game.exe'
  Format = "%[4]s"

[[OverridesX]]
  Name = "wine64-preloader"
  Regex = '^(.*(/|\\))?([^/]+)\\Game.exe'
  Format = "%[4]s"

[[OverridesX]]
  Name = "wine64-preloader"
  Regex = '^(.*(/|\\))?([^/\ ]+)\.exe'
  Format = "%[4]s"

[[OverridesX]]
  Name = "wine64-preloader"
  Delegate = "/home/desuwa/.config/screenshotter/wine-preloader.sh"

[[OverridesX]]
  Name = "wine-preloader"
  Regex = '^(.*(/|\\))?([^/\ ]+)\.exe'
  Format = "%[4]s"

[[OverridesX]]
  Name = "nw"
  Regex = '^(.*(/))?([^/]+) V\.\d+\(:alpha:\) Linux/nw'
  Format = "%[3]s"

[[OverridesX]]
  Name = "nw"
  Regex = '^(.*(/))?([^/]+)/nw'
  Format = "%[3]s"

[[OverridesX]]
  Name = "aw-man"
  Delegate = "/home/desuwa/.config/screenshotter/aw-man-manga-dir.sh"

[[OverridesX]]
  Name = "aw-man"
  Yearly = true

[[OverridesX]]
  Regex = '^/usr/bin/python(\d(.\d)?)? .*/\.?bin/([^/ ]+)( .*)?'
  Format = "%[4]s"

[[OverridesX]]
  Name = 'java'
  Regex = 'runelite'
  Format = "runelite"

[[OverridesX]]
  Name = 'java'
  Regex = 'minecraft-[0-9.]*-client.jar'
  Format = "minecraft"

[[OverridesX]]
  Name = "watch"
  Regex = '^watch (\-n [^ ]+ )?([^ ]+)?'
  Format = "%[3]s"

[[OverridesX]]
  Name = "firefox"
  Delegate = "/home/desuwa/.config/screenshotter/firefox-mangadex.sh"

[[OverridesX]]
  Name = "mpv"
  Delegate = "/home/desuwa/.config/screenshotter/mpv-anime.sh"

[[OverridesX]]
  Name = "firefox"
  Yearly = true

[[OverridesX]]
  Name = "mpv"
  Yearly = true

[[OverridesX]]
  Name = "wine-preloader"
  Regex = 'Among Us.exe$'
  Format = "Among Us"

[[OverridesX]]
  Name = "wine-preloader"
  Regex = 'noratoto1.exe$'
  Format = "Nora to Oujo to Noraneko Heart"

[[OverridesX]]
  Name = "ppssppqt"
  Delegate = "/home/desuwa/.config/screenshotter/ppsspp.sh"
  Callback = "/home/desuwa/.config/screenshotter/ppsspp-callback.sh"

[[OverridesX]]
  Name = "ppssppsdl"
  Delegate = "/home/desuwa/.config/screenshotter/ppssppsdl.sh"

[[OverridesX]]
  Name = "duckstation-qt"
  Delegate = "/home/desuwa/.config/screenshotter/duckstation.sh"

[[OverridesX]]
  Name = "kthreadd"
  Delegate = "/home/desuwa/.config/screenshotter/kthreadd.sh"


[[overrides]]
  name = "vimx"
  format = "vim"

[[overrides]]
  name = "gvim"
  format = "vim"

[[overrides]]
  name = "nvim"
  format = "vim"

[[overrides]]
  name = "chromium-browser"
  format = "chromium"

[[overrides]]
  regex = '(E|e)muera'
  format = "emuera"

[[overrides]]
  name = "wine32-preloader"
  regex = 'TLS.*Game.exe$'
  format = "the-last-sovereign"

# [[overrides]]
#   name = "wine32-preloader"
#   regex = '^(.*(/|\\))?([^/]+) \d\.\d\d\d[[:lower:]]?\\Game.exe'
#   format = "%[4]s"
#
# [[overrides]]
#   name = "wine32-preloader"
#   regex = '^(.*(/|\\))?([^/]+) ?\d\.?\d\.?\d[[:lower:]]?\\Game.exe'
#   format = "%[4]s"
#
# [[overrides]]
#   name = "wine32-preloader"
#   regex = '^(.*(/|\\))?([^/]+)\\Game.exe'
#   format = "%[4]s"
#
# [[overrides]]
#   name = "wine32-preloader"
#   regex = '^(.*(/|\\))?([^/\ ]+)\.exe'
#   format = "%[4]s"
#
# [[overrides]]
#   name = "wine64-preloader"
#   regex = '^(.*(/|\\))?([^/]+) \d\.\d\d\d[[:lower:]]?\\Game.exe'
#   format = "%[4]s"
#
# [[overrides]]
#   name = "wine64-preloader"
#   regex = '^(.*(/|\\))?([^/]+) ?\d\.?\d\.?\d[[:lower:]]?\\Game.exe'
#   format = "%[4]s"
#
# [[overrides]]
#   name = "wine-preloader"
#   regex = '^(.*(/|\\))?([^/]+) v?(\d+\.)*\d+[[:lower:]]?\\Game.exe'
#   format = "%[4]s"
#
# [[overrides]]
#   name = "wine-preloader"
#   regex = '^(.*(/|\\))?([^/]+) (\d+\.)*\d+[[:lower:]]?\\Game.exe'
#   format = "%[4]s"
#
# [[overrides]]
#   name = "wine64-preloader"
#   regex = '^(.*(/|\\))?([^/]+)\\Game.exe'
#   format = "%[4]s"
#
# [[overrides]]
#   name = "wine64-preloader"
#   regex = '^(.*(/|\\))?([^/\ ]+)\.exe'
#   format = "%[4]s"
#
# [[overrides]]
#   name = "wine-preloader"
#   regex = '^(.*(/|\\))?([^/\ ]+)\.exe'
#   format = "%[4]s"
#
[[overrides]]
  name = "nw"
  regex = '^(.*(/))?([^/]+) V\.\d+\(:alpha:\) Linux/nw'
  format = "%[3]s"

[[overrides]]
  name = "nw"
  regex = '^(.*(/))?([^/]+)/nw'
  format = "%[3]s"

[[overrides]]
  name = "aw-man"
  delegate = "/home/desuwa/.config/screenshotter/aw-man-manga-dir.sh"

[[overrides]]
  name = "aw-man"
  yearly = true

[[overrides]]
  regex = '^/usr/bin/python(\d(.\d)?)? .*/\.?bin/([^/ ]+)( .*)?'
  format = "%[4]s"

[[overrides]]
  name = 'java'
  regex = 'runelite'
  format = "runelite"

[[overrides]]
  name = 'java'
  regex = 'minecraft-[0-9.]*-client.jar'
  format = "minecraft"

[[overrides]]
  name = "watch"
  regex = '^watch (\-n [^ ]+ )?([^ ]+)?'
  format = "%[3]s"

[[overrides]]
  name = "firefox"
  delegate = "/home/desuwa/.config/screenshotter/firefox-mangadex.sh"

[[overrides]]
  name = "mpv"
  delegate = "/home/desuwa/.config/screenshotter/mpv-anime.sh"

[[overrides]]
  name = "firefox"
  yearly = true

[[overrides]]
  name = "mpv"
  yearly = true

[[overrides]]
  name = "wine-preloader"
  regex = 'Among Us.exe$'
  format = "Among Us"

[[overrides]]
  name = "ppssppqt"
  delegate = "/home/desuwa/.config/screenshotter/ppsspp.sh"
  callback = "/home/desuwa/.config/screenshotter/ppsspp-callback.sh"

[[overrides]]
  name = "ppssppsdl"
  delegate = "/home/desuwa/.config/screenshotter/ppssppsdl.sh"

[[overrides]]
  name = "duckstation-qt"
  delegate = "/home/desuwa/.config/screenshotter/duckstation.sh"

[[overrides]]
  name = "kthreadd"
  delegate = "/home/desuwa/.config/screenshotter/kthreadd.sh"
