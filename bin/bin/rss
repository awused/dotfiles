#!/bin/sh

e=$(which webnovel-rss)
[ $? = 0 ] || go install github.com/awused/rss-scrapers/webnovel-rss

e=$(which gelbooru-rss)
[ $? = 0 ] || go install github.com/awused/rss-scrapers/gelbooru-rss

go run -race $GOPATH/src/github.com/awused/aw-rss -awconf $HOME/.config/aw-rss/dev.toml
