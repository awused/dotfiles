#!/bin/sh

tempfile=/tmp/.redshift-disabled

if [ "$1" == "true" ] || [ "$1" == "" ] && [ -f "$tempfile" ]; then
  rm "$tempfile"
  pkill -x -USR1 redshift
elif [ "$1" == "false" ] || [ "$1" == "" ] && [ ! -f "$tempfile" ]; then
  touch "$tempfile"
  pkill -x -USR1 redshift
fi
